require 'rubygems'
require 'bundler/setup'
Bundler.require :default

ROOT_DIR = File.dirname(__FILE__)

HULL = Rake::FileList['docker_images/hull', 'docker_images/hull' + "/**/*"]

namespace :bl do
	desc "CTR"
	task :waypoint => [:deckhouse] do
		puts "OK"
	end

	desc "CTR"
	task :deckhouse => [HULL] do
		puts "OK"
	end
end