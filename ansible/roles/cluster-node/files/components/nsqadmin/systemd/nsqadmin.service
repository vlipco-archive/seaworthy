[Unit]

Description=Nsq admin web UI
Requires=vlipco.target
Wants=lookupd.service

[Service]

Restart=always
RestartSec=5
TimeoutStopSec=15
ExecStart=/bin/bash -c "/usr/local/bin/nsqadmin `lookupd-params http`"

[Install]

WantedBy=vlipco.target
