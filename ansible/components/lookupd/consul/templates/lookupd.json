{
    "service": {
        "name": "lookupd",
        "id": "{{inventory_hostname}}-lookupd",
        "check": {
            "notes": "Every 30s checks lookupd ping endpoint",
            "script": "{{checks_dir}}/lookupd",
            "interval": "30s"
        }
    }
}