{
    "service": {
        "name": "nsqevents",
        "check": {
            "notes": "Every 30s checks nsqevent ping writes to the tail of a file",
            "script": "/usr/cluster/checks/nsqevents",
            "interval": "30s"
        }
    }
}