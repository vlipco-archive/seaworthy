{
    "service": {
        "name": "waypoint",
        "check": {
            "notes": "Every 30s checks that waypoint's slug server is running",
            "script": "/var/checks/waypoint",
            "interval": "10s"
        }
    }
}