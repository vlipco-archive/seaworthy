[Unit]

Description=Nsq admin web UI
Requires=cluster.target
Wants=lookupd.service

[Service]

Restart=always
RestartSec=5
TimeoutStopSec=15
ExecStart=/bin/bash -c "/usr/bin/nsqadmin `lookupd-params http`"

[Install]
WantedBy=cluster.target
